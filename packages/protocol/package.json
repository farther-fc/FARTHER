{
  "name": "protocol",
  "version": "1.0.0",
  "description": "",
  "exports": "./index.js",
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "deploy:staging": "EVM_NETWORK=sepolia source .env && forge script script/Deploy.s.sol:Deploy --rpc-url $SEPOLIA_RPC_URL --private-key $PRIVATE_KEY --verify -vvvv --broadcast",
    "build": "forge build --out ../common/src/abis",
    "generate": "dotenv -e ../../.env wagmi generate",
    "postinstall": "pnpm build && pnpm generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@wagmi/cli": "^2.1.4",
    "viem": "^2.8.14",
    "wagmi": "^2.5.11"
  }
}
